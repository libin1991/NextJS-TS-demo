import * as React from 'react';
import Layout from '../components/Layout';

const AboutPage: React.FunctionComponent = () => (
  <Layout>
    <h1>Nothing here.</h1>
    <p>Just wanted to test the router ;)</p>
  </Layout>
);

export default AboutPage;
